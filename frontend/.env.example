# Backend API Base URL
VITE_API_BASE_URL=http://localhost:8000

# Mapbox GL Access Token (Optional: Falls back to Carto Dark Matter basemap if omitted)
VITE_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_token_here
